$(document).ready(function () {
    $("button").click(function () {
        // let generator = Math.ceil(Math.random () * 10);
        // let generator = Math.random () * 10;
        $("#numberDisplay").text(generator);
    });
});